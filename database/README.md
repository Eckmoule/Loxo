# Database Migrations

## Structure

Les migrations sont numérotées séquentiellement : `001_`, `002_`, etc.

## Exécution

1. Copier le contenu de la migration
2. Aller dans Supabase → SQL Editor
3. Coller et exécuter

## Migrations appliquées

- [x] `001_create_contact_messages.sql` - Table des messages de contact (2026-04-23)
